[flake8]
max-line-length = 120
exclude = venv,.git,__pycache__,.eggs
per-file-ignores =
    __init__.py:F401

[tool:pytest]
testpaths = tests
addopts = -v --tb=short

[coverage:run]
source = .
omit = tests/*,venv/*,setup.py

[coverage:report]
show_missing = true
fail_under = 50
